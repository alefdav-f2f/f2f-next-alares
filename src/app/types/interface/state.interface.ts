export interface StateProps {
    createdAt: string
    id: string
    isActive: boolean
    name: string
    updatedAt: string
    uf: string
}

export interface CityProps {
    id: string
    name: string
    slug: string
    value: string
}